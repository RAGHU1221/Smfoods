name: Build Sri Murugan Foods APK

on:
  workflow_dispatch:

  push:
    branches:
      - main
      - master

permissions:
  contents: read

jobs:
  build-apk:

    name: Build Android APK
    runs-on: ubuntu-latest

    steps:

      # ==========================================
      # 1. Checkout
      # ==========================================
      - name: Checkout source
        uses: actions/checkout@v4


      # ==========================================
      # 2. Node.js
      # ==========================================
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm


      # ==========================================
      # 3. Java 17
      # ==========================================
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'


      # ==========================================
      # 4. Environment
      # ==========================================
      - name: Check environment
        run: |
          echo "======================================"
          echo "Environment"
          echo "======================================"

          node --version
          npm --version
          java -version


      # ==========================================
      # 5. Install dependencies
      # ==========================================
      - name: Install dependencies
        run: |
          npm install --no-audit --no-fund


      # ==========================================
      # 6. Install Capacitor
      # ==========================================
      - name: Install Capacitor
        run: |
          npm install \
            @capacitor/core@7.6.9 \
            @capacitor/cli@7.6.9 \
            @capacitor/android@7.6.9 \
            --save \
            --no-audit \
            --no-fund


      # ==========================================
      # 7. Remove old Capacitor configs
      # ==========================================
      - name: Remove old Capacitor configs
        run: |
          rm -f capacitor.config.ts
          rm -f capacitor.config.js
          rm -f capacitor.config.cjs
          rm -f capacitor.config.json

          echo "Old Capacitor config files removed."


      # ==========================================
      # 8. Create Capacitor JSON config
      # ==========================================
      - name: Create Capacitor config
        run: |

          cat > capacitor.config.json <<'EOF'
          {
            "appId": "com.srimuruganfoods.pos",
            "appName": "Sri Murugan Foods POS",
            "webDir": "dist",
            "bundledWebRuntime": false,
            "android": {
              "backgroundColor": "#b91c1c"
            }
          }
          EOF

          echo "======================================"
          echo "Capacitor config created"
          echo "======================================"

          cat capacitor.config.json


      # ==========================================
      # 9. Verify appId
      # ==========================================
      - name: Verify Capacitor config
        run: |

          if [ ! -f "capacitor.config.json" ]; then
            echo "ERROR: capacitor.config.json not found."
            exit 1
          fi

          echo ""
          echo "Checking appId..."

          grep -q '"appId": "com.srimuruganfoods.pos"' capacitor.config.json

          if [ $? -ne 0 ]; then
            echo "ERROR: appId is missing."
            exit 1
          fi

          echo "appId verified:"
          echo "com.srimuruganfoods.pos"


      # ==========================================
      # 10. Verify Capacitor CLI
      # ==========================================
      - name: Verify Capacitor
        run: |

          echo "======================================"
          echo "Capacitor version"
          echo "======================================"

          npx cap --version


      # ==========================================
      # 11. Build web app
      # ==========================================
      - name: Build web application
        run: |

          echo "======================================"
          echo "Building web application"
          echo "======================================"

          npm run build


      # ==========================================
      # 12. Verify dist
      # ==========================================
      - name: Verify web build
        run: |

          if [ ! -d "dist" ]; then
            echo "ERROR: dist folder was not created."
            exit 1
          fi

          echo "======================================"
          echo "Web build successful"
          echo "======================================"

          ls -lah dist


      # ==========================================
      # 13. Create Android
      # ==========================================
      - name: Create Android platform
        shell: bash
        run: |

          if [ -d "android" ]; then

            echo "Android project already exists."

          else

            echo "======================================"
            echo "Android project not found."
            echo "Creating Android project..."
            echo "======================================"

            npx cap add android

          fi


      # ==========================================
      # 14. Verify Android
      # ==========================================
      - name: Verify Android platform
        shell: bash
        run: |

          if [ ! -d "android" ]; then
            echo "ERROR: Android folder was not created."
            exit 1
          fi

          if [ ! -f "android/gradlew" ]; then
            echo "ERROR: Gradle wrapper not found."
            exit 1
          fi

          if [ ! -d "android/app" ]; then
            echo "ERROR: Android app module not found."
            exit 1
          fi

          echo "======================================"
          echo "Android project verified"
          echo "======================================"


      # ==========================================
      # 15. Capacitor sync
      # ==========================================
      - name: Capacitor sync
        run: |

          echo "======================================"
          echo "Capacitor Sync"
          echo "======================================"

          npx cap sync android


      # ==========================================
      # 16. Setup Gradle
      # ==========================================
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4


      # ==========================================
      # 17. Make Gradle executable
      # ==========================================
      - name: Make Gradle executable
        working-directory: android
        run: |

          chmod +x gradlew

          echo "Gradle executable permission added."


      # ==========================================
      # 18. Build Debug APK
      # ==========================================
      - name: Build Debug APK
        working-directory: android
        run: |

          echo "======================================"
          echo "Building Android APK"
          echo "======================================"

          ./gradlew assembleDebug \
            --no-daemon \
            --stacktrace


      # ==========================================
      # 19. Find APK
      # ==========================================
      - name: Verify APK
        shell: bash
        run: |

          APK="android/app/build/outputs/apk/debug/app-debug.apk"

          echo "======================================"
          echo "Checking APK"
          echo "======================================"

          if [ ! -f "$APK" ]; then

            echo "ERROR: APK was not generated."

            echo ""
            echo "Searching for APK files..."

            find android -type f -name "*.apk" -print

            exit 1

          fi

          echo ""
          echo "APK BUILD SUCCESSFUL"
          echo ""

          ls -lh "$APK"


      # ==========================================
      # 20. Upload APK
      # ==========================================
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Sri-Murugan-Foods-POS-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error


      # ==========================================
      # 21. Final summary
      # ==========================================
      - name: Build summary
        run: |

          echo ""
          echo "=============================================="
          echo " Sri Murugan Foods POS"
          echo " Android APK Build"
          echo "=============================================="
          echo ""
          echo "App Name : Sri Murugan Foods POS"
          echo "App ID   : com.srimuruganfoods.pos"
          echo "Build    : Debug APK"
          echo ""
          echo "APK Location:"
          echo "android/app/build/outputs/apk/debug/app-debug.apk"
          echo ""
          echo "=============================================="
          echo " BUILD COMPLETED SUCCESSFULLY"
          echo "=============================================="
